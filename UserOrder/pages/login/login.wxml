<!--pages/login/login.wxml-->
<view class="container">

  <!--按钮-->
  <view class="loginBtnView">
    <button class="loginBtn" open-type='getUserInfo' size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}" disabled="{{disabled}}" bindgetuserinfo="bindGetUserInfo">扫码点餐</button>
  </view>
</view>